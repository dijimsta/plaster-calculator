query ListQuestionnaireTemplates($ownerId: String!) @auth(level: NO_ACCESS) {
    questionnaireTemplates(
        where: { ownerId: { eq: $ownerId } }
        orderBy: [{ updatedAt: DESC }]
    ) {
        id
        name
        createdAt
        updatedAt
    }
}
