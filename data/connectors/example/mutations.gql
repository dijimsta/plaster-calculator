# Example mutations for a simple movie app

# Create a movie based on user input
mutation CreateMovie($title: String!, $genre: String!, $imageUrl: String!)
@auth(
    level: USER_EMAIL_VERIFIED
    insecureReason: "Any email verified users can create a new movie."
) {
    movie_insert(data: { title: $title, genre: $genre, imageUrl: $imageUrl })
}

# Delete a movie by id. This operation is intended to be called only from
# trusted Cloud Functions through the generated Admin SDK.
mutation DeleteMovie($id: UUID!) @auth(level: NO_ACCESS) {
    movie_delete(id: $id)
}

# Upsert (update or insert) a user's username based on their auth.uid
mutation UpsertUser($username: String!) @auth(level: USER) {
    # The "auth.uid" server value ensures that users can only register their own user.
    user_upsert(data: { id_expr: "auth.uid", username: $username })
}

# Add a review for a movie
mutation AddReview($movieId: UUID!, $rating: Int!, $reviewText: String!)
@auth(level: USER) {
    review_upsert(
        data: {
            userId_expr: "auth.uid"
            movieId: $movieId
            rating: $rating
            reviewText: $reviewText
            # reviewDate defaults to today in the schema. No need to set it manually.
        }
    )
}

# Logged in user can delete their review for a movie
mutation DeleteReview($movieId: UUID!) @auth(level: USER) {
    # The "auth.uid" server value ensures that users can only delete their own reviews.
    review_delete(key: { userId_expr: "auth.uid", movieId: $movieId })
}

mutation CreatePlanFromUpload(
    $ownerId: String!
    $name: String!
    $originalFileName: String!
    $uploadType: String!
    $originalPath: String!
    $status: String!
    $pageCount: Int!
) @auth(level: NO_ACCESS) {
    plan_insert(
        data: {
            ownerId: $ownerId
            name: $name
            originalFileName: $originalFileName
            uploadType: $uploadType
            originalPath: $originalPath
            status: $status
            pageCount: $pageCount
            createdAt_expr: "request.time"
            updatedAt_expr: "request.time"
        }
    )
}

mutation RenamePlan($id: UUID!, $name: String!) @auth(level: NO_ACCESS) {
    plan_update(
        id: $id
        data: { name: $name, updatedAt_expr: "request.time" }
    )
}

mutation TouchPlan($id: UUID!, $status: String, $processingError: String)
@auth(level: NO_ACCESS) {
    plan_update(
        id: $id
        data: {
            status: $status
            processingError: $processingError
            updatedAt_expr: "request.time"
        }
    )
}

mutation DeletePlanPages($planId: UUID!) @auth(level: NO_ACCESS) {
    planPage_deleteMany(where: { planId: { eq: $planId } })
}

mutation DeletePlan($id: UUID!) @auth(level: NO_ACCESS) {
    plan_delete(id: $id)
}

mutation CreatePlanPage(
    $planId: UUID!
    $pageNumber: Int!
    $status: String!
    $sourceImagePath: String
    $previewImagePath: String
    $overlayJson: String
    $scaleMmPerPx: Float
    $ceilingHeightMm: Float
    $referencePointsJson: String
    $referenceLengthMm: Float
    $processingStrategy: String
    $processingMetadataJson: String
) @auth(level: NO_ACCESS) {
    planPage_insert(
        data: {
            planId: $planId
            pageNumber: $pageNumber
            status: $status
            sourceImagePath: $sourceImagePath
            previewImagePath: $previewImagePath
            overlayJson: $overlayJson
            scaleMmPerPx: $scaleMmPerPx
            ceilingHeightMm: $ceilingHeightMm
            referencePointsJson: $referencePointsJson
            referenceLengthMm: $referenceLengthMm
            processingStrategy: $processingStrategy
            processingMetadataJson: $processingMetadataJson
            createdAt_expr: "request.time"
            updatedAt_expr: "request.time"
        }
    )
}

mutation UpdatePlanPage(
    $id: UUID!
    $overlayJson: String
    $scaleMmPerPx: Float
    $ceilingHeightMm: Float
    $referencePointsJson: String
    $referenceLengthMm: Float
) @auth(level: NO_ACCESS) {
    planPage_update(
        id: $id
        data: {
            overlayJson: $overlayJson
            scaleMmPerPx: $scaleMmPerPx
            ceilingHeightMm: $ceilingHeightMm
            referencePointsJson: $referencePointsJson
            referenceLengthMm: $referenceLengthMm
            updatedAt_expr: "request.time"
        }
    )
}

mutation UpdatePlanPages(
    $planId: UUID!
    $scaleMmPerPx: Float
    $ceilingHeightMm: Float
) @auth(level: NO_ACCESS) {
    planPage_updateMany(
        where: { planId: { eq: $planId } }
        data: {
            scaleMmPerPx: $scaleMmPerPx
            ceilingHeightMm: $ceilingHeightMm
            updatedAt_expr: "request.time"
        }
    )
}
