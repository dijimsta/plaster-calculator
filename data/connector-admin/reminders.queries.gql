query ListDueReminders($teamId: String!) @auth(level: NO_ACCESS) {
    reminders(
        where: {
            teamId: { eq: $teamId }
            status: { eq: "OPEN" }
            dueAt: { le_expr: "request.time" }
        }
        orderBy: [{ dueAt: ASC }]
    ) {
        id
        teamId
        projectId
        accountId
        name
        status
        dueAt
        completedAt
        createdAt
        updatedAt
    }
}

query ListProjectReminders($projectId: UUID!) @auth(level: NO_ACCESS) {
    reminders(
        where: { projectId: { eq: $projectId } }
        orderBy: [{ dueAt: ASC }]
    ) {
        id
        teamId
        projectId
        accountId
        name
        status
        dueAt
        completedAt
        createdAt
        updatedAt
    }
}

query GetReminderById($id: UUID!) @auth(level: NO_ACCESS) {
    reminder(id: $id) {
        id
        teamId
        projectId
        accountId
        name
        status
        dueAt
        completedAt
        createdAt
        updatedAt
    }
}
