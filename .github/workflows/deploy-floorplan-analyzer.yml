name: Deploy Floorplan Analyzer Functions

on:
    workflow_dispatch:

concurrency:
    group: deploy-floorplan-analyzer-functions
    cancel-in-progress: false

jobs:
    deploy:
        runs-on: ubuntu-latest
        timeout-minutes: 360
        environment:
            name: floorplan-analyzer-functions
            url: https://console.firebase.google.com/project/plaster-calculator/functions
        steps:
            - uses: actions/checkout@v6

            - uses: pnpm/action-setup@v4
              with:
                  version: 11.1.3

            - uses: actions/setup-node@v6
              with:
                  node-version: 24
                  cache: pnpm

            - uses: astral-sh/setup-uv@v8.1.0

            - name: Install dependencies
              run: pnpm install --frozen-lockfile

            - name: Deploy functions
              run: pnpm exec firebase deploy --only functions:floorplan-analyzer --project plaster-calculator --token "$FIREBASE_TOKEN"
              env:
                  FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
