/** A single clarification shown in the save-as-template modal's read-only list. */
export type SaveQuestionnaireTemplateFromProjectModalQuestion = {
    readonly label: string;
    readonly isFromSourceTemplate: boolean;
};
