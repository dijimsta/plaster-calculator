{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "command": "pnpm exec nx run-many -t build",
            "label": "build",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$tsc"]
        }
    ]
}
