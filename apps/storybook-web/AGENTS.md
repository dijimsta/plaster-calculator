# storybook-web

Inherits the [repository guidelines](../../AGENTS.md), [TypeScript guidelines](../../docs/typescript-guidelines.md),
[web UI guidelines](../../docs/web-ui-guidelines.md), and [service architecture](../../docs/service-architecture.md).

- Keep stories under `src/stories/`, organised by public component or domain namespace.
- Run `pnpm --filter @apps/storybook-web build` in addition to the root checks when changing Storybook configuration.
